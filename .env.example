APP_NAME=SkyLedger
SECRET_KEY=change-this-before-deploy
FLASK_HOST=127.0.0.1
FLASK_PORT=5000
FLASK_DEBUG=1

# Local development defaults.
PGHOST=localhost
PGPORT=5432
PGDATABASE=cs307_project
PGUSER=cs307
PGPASSWORD=cs307

# Production deployment can use a single DSN instead.
# DATABASE_URL=postgresql://user:password@db-host:5432/cs307_project

# Domain name used by Caddy in docker-compose.
DOMAIN=example.com
